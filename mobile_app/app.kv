#:kivy 2.3.0

<ShopNeraScreenManager>:
    HomeScreen:
    ShopScreen:
    CartScreen:
    ProfileScreen:
    RiderScreen:

<HomeScreen>:
    name: "home"
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "🏪 Welcome to ShopNera!"
            font_size: 28
        Button:
            text: "Go to Shops"
            size_hint_y: 0.2
            on_release:
                app.root.current = "shop"

<ShopScreen>:
    name: "shop"
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "🛍 Shops Near You"
            font_size: 24
        Button:
            text: "Go to Cart"
            on_release:
                app.root.current = "cart"
        Button:
            text: "Back to Home"
            on_release:
                app.root.current = "home"

<CartScreen>:
    name: "cart"
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "🛒 Your Cart"
            font_size: 24
        Button:
            text: "Profile"
            on_release:
                app.root.current = "profile"
        Button:
            text: "Back to Shop"
            on_release:
                app.root.current = "shop"

<ProfileScreen>:
    name: "profile"
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "👤 Profile Page"
            font_size: 24
        Button:
            text: "Rider Portal"
            on_release:
                app.root.current = "rider"
        Button:
            text: "Back to Cart"
            on_release:
                app.root.current = "cart"

<RiderScreen>:
    name: "rider"
    BoxLayout:
        orientation: "vertical"
        Label:
            text: "🚴‍♂️ Rider Dashboard"
            font_size: 24
        Button:
            text: "Back to Profile"
            on_release:
                app.root.current = "profile"
